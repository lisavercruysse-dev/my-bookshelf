services:
  mybookshelf-frontend:
    image: mybookshelf-frontend
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        VITE_PERSONAL_API_URL: http://localhost:3000/api
    container_name: budget-app-frontend
    ports:
      - '80:80'
